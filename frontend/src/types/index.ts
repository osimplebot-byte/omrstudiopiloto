export interface AvaliacaoResumo {
  id: string;
  turma: string;
  disciplina: string;
  data: string;
  etapa?: string;
}

export interface Persona {
  id: string;
  nome: string;
  descricao: string;
}
